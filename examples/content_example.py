# -*- coding: utf-8 -*-
"""内容表示例 —— 复制后改成自己的内容即可（docx-writer）

构建：
    python scripts/build_docx.py --base 模板.docx --out 目标.docx --content 本文件.py \
        --cover "系统名|文档类型|系统名|2026年9月" \
        --revision "2026-09-14|1.0|初版|项目组" \
        --header "系统名 - 文档类型" --title "系统名-文档类型"
"""
AVAIL = 8306            # 版心宽 twip = A4(11906) − 左右页边距(1800×2)

CONTENT = [
    # ---- 第 1 章 ----
    ('H1', '编写说明'),                       # ★ 不要写编号，编号由基底的 numbering 自动生成
    ('H2', '1.1 目的与范围'),                  # ★ 节标题的编号写在文本里
    ('P',  '本文件用于说明……（正文自动首行缩进 2 汉字、固定行距 20 磅）。'),
    ('P',  '第二段正文。'),
    ('H2', '1.2 术语与缩略语'),
    ('TBLCAP', '表1-1 术语与说明'),            # 表题：自动置于表格上方、居中
    ('T', ['术语', '说明'],
          [['内容表', '描述文档正文的元组列表'],
           ['基底', '提供编号/样式/页眉页脚/节的源文档']],
          [2000, 6306]),                      # 列宽合计必须 == AVAIL

    # ---- 第 2 章 ----
    ('H1', '操作步骤'),
    ('H2', '2.1 启动服务'),
    ('P',  '在部署主机上以如下命令启动常驻服务。'),
    ('C',  '$ systemctl restart road-network-main'),   # 命令段：左缩进、不首行缩进
    ('H2', '2.2 查看日志'),
    ('C',  '$ journalctl -u road-network-main -n 200 --no-pager'),

    # ---- 第 3 章：插图 ----
    ('H1', '附录'),
    ('H2', '3.1 系统架构'),
    ('P',  '系统总体架构如图3-1所示。'),
    # 三种写法任选：
    #   ('FIG', 路径)                  默认等比缩放到版心宽（不放大）
    #   ('FIG', 路径, 380)             指定显示宽度 380 磅
    #   ('FIG', 路径, 380, '图3-1 …')  插图 + 图题一步到位（推荐）
    ('FIG', r'assets\arch.png', 380, '图3-1 系统总体架构'),
    ('H2', '3.2 联系方式'),
    ('TBLCAP', '表3-1 技术支持联系方式'),
    ('T', ['主机', '账号', '邮箱'],
          [['192.0.2.10', 'example-user', 'user@example.invalid']],
          [2400, 1900, 4006]),
    # 也可以分开写：先 ('FIG', 路径)，再单独一行 ('FIGCAP', '图3-2 xxx')
]
